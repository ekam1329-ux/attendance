# Server Configuration
PORT=5000

# Authentication (optional strict mode)
REQUIRE_AUTH=false
JWT_SECRET=phd-attendance-secret-key-2026-change-in-production

# Database Path (defaults to attendance.sqlite in backend root)
DATABASE_PATH=attendance.sqlite
